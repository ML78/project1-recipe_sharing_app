<h1>Categories</h1>

<% @categories.each do |category| %>
  <h3> <%= link_to category.title, category %> </h3>
<% end %>

<% if @current_user.present? && @current_user.admin %>
  <p><%= link_to "Add a new category", new_category_path, :class => 'btn btn-warning' %></p>
<% end %>
